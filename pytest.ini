[pytest]
asyncio_mode = auto
python_files = test_*.py
addopts = -ra
markers =
    slow: controlled stability or load test; excluded from quick checks
    visual: document rendering or visual-layout check
    live_ai: uses a real model key and never runs on pull requests
    security: security and privacy regression test
