# MateMaTeX 2.0 Backend
# AI Pipeline
langgraph>=1.0.0,<2.0.0
langchain-core>=1.2.0,<2.0.0
langchain-google-genai>=4.1.0,<5.0.0
langchain-anthropic>=1.3.0,<2.0.0
langchain-openai>=1.1.0,<2.0.0

# Math verification
sympy>=1.13.0
antlr4-python3-runtime==4.13.2  # Needed by SymPy's LaTeX parser (suppresses warning)

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
slowapi>=0.1.9
sse-starlette>=2.0.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Database (PostgreSQL)
asyncpg>=0.30.0

# LaTeX
# pdflatex is a system dependency (installed via TeX Live in Docker)

# Utilities
python-dotenv>=1.0.0
structlog>=24.0.0

# Document export
python-docx>=1.1.0
latex2mathml>=3.77.0
mathml2omml>=0.0.2
python-pptx>=0.6.23

# Image / QR
qrcode[pil]>=7.4.0
Pillow>=10.0.0

# Async HTTP (embedding API calls)
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Password hashing (share links)
bcrypt>=4.2.0

# JWT (Supabase auth)
PyJWT>=2.8.0
